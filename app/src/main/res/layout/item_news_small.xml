<?xml version="1.0" encoding="utf-8"?>
<!-- item_news_small.xml — Full-width, no rounded corners, balanced admin list item -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="1dp"
    android:background="#1E2A38"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:orientation="horizontal"
    android:padding="14dp"
    android:gravity="center_vertical">

    <!-- Thumbnail nhỏ bên trái — tỉ lệ 4:3 cố định -->
    <ImageView
        android:id="@+id/imgNewsThumb"
        android:layout_width="100dp"
        android:layout_height="75dp"
        android:scaleType="centerCrop"
        android:contentDescription="Ảnh bài viết" />

    <!-- Nội dung bên phải -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <!-- Badge danh mục -->
        <TextView
            android:id="@+id/tvNewsCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:background="@drawable/bg_category_green"
            android:text="Danh mục"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp"/>

        <!-- Tiêu đề -->
        <TextView
            android:id="@+id/tvNewsTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:lineSpacingExtra="2dp"
            android:maxLines="1"
            android:text="Tiêu đề bài viết nhỏ hiển thị ở đây"
            android:textColor="#EEEEEE"
            android:textSize="13sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp"/>

        <!-- Tác giả -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="👤 "
                android:textSize="10sp"/>

            <TextView
                android:id="@+id/tvNewsAuthor"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="Admin"
                android:textColor="#888888"
                android:textSize="11sp"/>

        </LinearLayout>

        <!-- Ngày đăng -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="3dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🕐 "
                android:textSize="10sp"/>

            <TextView
                android:id="@+id/tvNewsDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="01/03/2026"
                android:textColor="#666666"
                android:textSize="11sp"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Nút Edit + Xóa xếp dọc -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="4dp">

        <!-- Nút Edit -->
        <ImageView
            android:id="@+id/btnSmallEdit"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:src="@drawable/ic_edit"
            android:contentDescription="Chỉnh sửa"
            app:tint="#9E9E9E" />

        <!-- Nút Xóa -->
        <ImageView
            android:id="@+id/btnSmallDelete"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:src="@drawable/ic_delete_24"
            android:contentDescription="Xóa bài viết"
            app:tint="#E53935" />

    </LinearLayout>

</LinearLayout>